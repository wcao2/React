import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// rendering App component into that element with a id root(grabing the id in index.html)
ReactDOM.render(<App />, document.getElementById('root'));

//app component being loaded in app.js


